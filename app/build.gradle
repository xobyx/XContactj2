apply plugin: 'com.android.application'


android {
    signingConfigs {
        config {
            keyAlias 'xobyx'
            keyPassword '13011221'
            storeFile file('C:/Users/xobyx/AndroidStudioProjects/XContactj2/pkey.jks')
            storePassword '13011221'
        }
    }
    lintOptions {
        checkReleaseBuilds false
        abortOnError false
        disable 'InvalidPackage'
    }
    compileSdkVersion 21
    buildToolsVersion "21.1.2"
    def Properties versionProps = new Properties()
    def versionPropsFile = file('version.properties')
    if (versionPropsFile.exists())
        versionProps.load(new FileInputStream(versionPropsFile))
    def code = (versionProps['VERSION_CODE'] ?: "0").toInteger() + 1
    versionProps['VERSION_CODE'] = code.toString()
    versionProps.store(versionPropsFile.newWriter(), null)
    defaultConfig {
        applicationId "xobyx.xcontactj"
        minSdkVersion 15
        targetSdkVersion 21
        versionCode code
        versionName "1.1"
        multiDexEnabled true
        signingConfig signingConfigs.config
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro', 'xcontactj-proguard.pro'
            signingConfig signingConfigs.config
            versionNameSuffix '-(' + defaultConfig.versionCode + ')-beta-aptoide'
        }
        debug {
        }
    }
    productFlavors {
    }
}

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    //compile 'frankiesardo:icepick:3.2.0'
    //provided 'frankiesardo:icepick-processor:3.2.0'
    compile 'com.android.support:appcompat-v7:22.2.0'
    compile 'com.android.support:design:22.2.0'
    compile 'com.android.support:recyclerview-v7:22.2.0'
    compile 'com.google.android.gms:play-services-analytics:8.1.0'
    compile 'com.google.android.gms:play-services-gcm:8.1.0'
    // compile 'com.github.lzyzsd:circleprogress:1.1.0'
    //  compile 'com.klinkerapps:android-chips:1.0.0'
    //compile 'me.leolin:ShortcutBadger:1.1.1'
    compile files('libs/libphonenumber-3.5.jar')
}







